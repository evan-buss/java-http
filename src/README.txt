The goal of this project is to simply receive an HTTP get request manipulate the
raw data without the use of an http library/framework. This will allow me to
understand exactly what is sent and all the pieces that go into it.